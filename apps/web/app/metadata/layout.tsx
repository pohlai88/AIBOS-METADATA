/**
 * Metadata Section Layout
 *
 * Shared layout for all metadata-related pages (glossary, lineage, quality).
 */

import type { ReactNode } from "react";

export default function MetadataLayout({ children }: { children: ReactNode }) {
  return children;
}
