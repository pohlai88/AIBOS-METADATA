# 🎨 Design-Specific MCP Tools Available

> **Your trained design AI agents are ready!**

---

## 🛠️ **Available Design MCP Servers**

### **1. `aibos-design-elegance-validator`** ⭐⭐⭐
**Purpose:** Validates design system against "Cockpit Elegance Standards"

**What it checks:**
- ✅ **Adaptive Luminance** - Light/dark mode color adaptation
- ✅ **Optical Physics** - Glass panels, blobs, border beams
- ✅ **Semantic Colors** - Status colors, theme colors
- ✅ **Unified System** - Animation consistency

**How to use:**
```
Use aibos-design-elegance-validator to audit design system
```

**Tools:**
- `audit_design_system` - Full design system audit
- `get_fix_snippets` - Get CSS fixes for issues

---

### **2. `aibos-ui-generator`** ⭐⭐⭐
**Purpose:** Generate UI components from natural language

**What it does:**
- Generates React/TypeScript components
- Enforces design tokens
- Uses Radix UI primitives
- Follows AI-BOS UI constitution

**How to use:**
```
Use aibos-ui-generator to generate a maze visualization component with 2D/3D support, crystal effects, and liquid animations
```

**Tools:**
- `generate_ui_layout` - Generate components from prompts

---

### **3. `aibos-component-generator`** ⭐⭐
**Purpose:** Component generation with constitution validation

**What it does:**
- Validates against UI constitution
- Checks design token usage
- Enforces accessibility
- Validates motion/animation

**How to use:**
```
Use aibos-component-generator to create a maze component
```

---

### **4. `aibos-a11y-validation`** ⭐⭐
**Purpose:** Accessibility validation

**What it checks:**
- WCAG compliance
- ARIA attributes
- Semantic HTML
- Keyboard navigation

---

### **5. `aibos-theme`** ⭐
**Purpose:** Theme management

**What it does:**
- Theme token management
- Color palette validation
- Design token consistency

---

## 🎯 **For Your Maze Concept:**

### **Use These Tools:**

1. **Generate the Maze Component:**
   ```
   Use aibos-ui-generator to generate:
   "A 2D/3D maze visualization component with:
   - Interactive maze paths (SVG or Canvas)
   - Crystal effects at intersections (glassmorphism)
   - Liquid flow animations through paths
   - Hover interactions that light up paths
   - Support for both 2D top-down and 3D isometric views
   - Framer Motion animations
   - Dark theme with cyan/teal/green accents"
   ```

2. **Validate Design Elegance:**
   ```
   Use aibos-design-elegance-validator to audit the maze component design
   ```

3. **Check Accessibility:**
   ```
   Use aibos-a11y-validation to ensure maze is accessible
   ```

---

## 🚀 **Quick Start:**

### **Generate Maze Component:**
```bash
# The MCP tools are already configured in .cursor/mcp.json
# Just ask me to use them:

"Use aibos-ui-generator to create a maze visualization component with 2D/3D support, crystal effects, and liquid animations for the landing page"
```

### **Validate Design:**
```bash
"Use aibos-design-elegance-validator to audit the design system and check if the maze component follows elegance standards"
```

---

## 📋 **What Each Tool Does:**

| Tool | Purpose | Best For |
|------|---------|----------|
| `aibos-design-elegance-validator` | Design system validation | Checking if design is "elegant" |
| `aibos-ui-generator` | Component generation | Creating new UI components |
| `aibos-component-generator` | Component validation | Validating existing components |
| `aibos-a11y-validation` | Accessibility | WCAG compliance |
| `aibos-theme` | Theme management | Color/token consistency |

---

**Status:** ✅ **All Design MCP Tools Ready!**

**Next:** Ask me to use `aibos-ui-generator` to create your maze component!


